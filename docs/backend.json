
{
  "entities": {
    "StockRecord": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StockRecord",
      "type": "object",
      "description": "Represents a record of a stock trade.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the stock record."
        },
        "dateTime": {
          "type": "string",
          "description": "The date and time the stock record was created, automatically fetched.",
          "format": "date-time"
        },
        "stockSymbol": {
          "type": "string",
          "description": "The stock symbol selected from a dynamic dropdown menu."
        },
        "entryPrice": {
          "type": "number",
          "description": "The price at which the stock was entered (in Rupees)."
        },
        "stopLoss": {
          "type": "number",
          "description": "The stop loss price for the stock (in Rupees)."
        },
        "targetPrice1": {
          "type": "number",
          "description": "The first target price for the stock (in Rupees)."
        },
        "targetPrice2": {
          "type": "number",
          "description": "The second optional target price for the stock (in Rupees)."
        },
        "targetPrice3": {
          "type": "number",
          "description": "The third optional target price for the stock (in Rupees)."
        },
        "positionalTargetPrice": {
          "type": "number",
          "description": "The optional positional target price for the stock (in Rupees)."
        },
        "notes": {
          "type": "string",
          "description": "Unique notes or thesis for this specific stock entry."
        }
      },
      "required": [
        "id",
        "dateTime",
        "stockSymbol",
        "entryPrice",
        "stopLoss",
        "targetPrice1"
      ]
    },
    "Journal": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Journal",
      "type": "object",
      "description": "Represents a user's single, general trading journal.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the journal entry, typically 'main'."
        },
        "content": {
          "type": "string",
          "description": "The markdown content of the user's journal."
        },
        "updatedAt": {
          "type": "string",
          "description": "The last time the journal was updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "content"
      ]
    },
    "UserSettings": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserSettings",
      "type": "object",
      "description": "Represents a user's application-specific settings.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the settings document, typically 'main'."
        },
        "riskPercentage": {
          "type": "number",
          "description": "The user's defined percentage loss per trade for position sizing calculations."
        },
        "capital": {
          "type": "number",
          "description": "The total trading capital available to the user."
        },
        "maxCapitalPercentagePerTrade": {
          "type": "number",
          "description": "The maximum percentage of total capital to be used in a single trade."
        }
      },
      "required": [
        "id"
      ]
    },
    "OptionChainCache": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OptionChainCache",
      "type": "object",
      "description": "Cached market data for option chains to bypass API throttling.",
      "properties": {
        "id": { "type": "string" },
        "snapshot": { "type": "object" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}/stockRecords/{stockRecordId}": {
      "schema": "#/backend/entities/StockRecord",
      "description": "Stores individual stock records for each user."
    },
    "/users/{userId}/journal/{journalId}": {
      "schema": "#/backend/entities/Journal",
      "description": "Stores a single, general-purpose trading journal for each user."
    },
    "/users/{userId}/settings/{settingId}": {
      "schema": "#/backend/entities/UserSettings",
      "description": "Stores a single document for user-specific settings."
    },
    "/optionChainData/{symbol}": {
      "schema": "#/backend/entities/OptionChainCache",
      "description": "A public cache for market data to ensure the UI stays populated even when Yahoo blocks specific clients."
    }
  }
}
